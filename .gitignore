# Build output
/target
dist
dist-portable

# CI-only fixture (generated by `scripts/gen-fixtures.py --large`)
tests-fixtures/pdf/large-500-pages.pdf

# Per-vault user data: search index, annotations, session state.
# Never repository content.
.md-editor/

# Editor and tooling state
.idea
.codex
.agents
