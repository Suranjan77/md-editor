[workspace]
members = ["core", "native"]
# v3 is an independent workspace (see docs/V3_GROUND_UP_PLAN.md / docs/V3_HANDOFF.md).
exclude = ["v3"]
resolver = "2"

[workspace.package]
version = "1.0.0"
edition = "2024"
license = "MIT"
